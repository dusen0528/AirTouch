# AirTouch 개발 규칙

- 사용자 요청에 따라 변경을 검증한 뒤 기능 단위로 커밋한다. 서로 다른 기능과 권한·설치 수정은 별도 커밋으로 나눈다.
- 커밋 메시지는 한글로 `유형(v버전·기능): 구체적인 변경` 형식을 사용한다. 예: `수정(v0.3.2·권한): 서명 변경 후 재등록 안내 보완`.
- 설치할 버전은 Info.plist에서 관리하고, 릴리스 검증 후 CHANGELOG.md와 `v버전` 태그를 갱신한다. 최초 소스 기준점은 v0.3.1이다. 소스가 보존되지 않은 이전 버전의 커밋을 재구성하지 않는다.
- 사용자가 실행할 앱은 `~/Applications/AirTouch.app` 하나만 유지한다. 설치 완료 후 임시 앱 패키지와 이전 실행 파일 백업을 정리한다. 이전 소스는 Git으로 보존한다.
- 번들 ID와 개발 서명 인증서를 유지한다. 권한 문제를 다룰 때는 docs/implementation-status.md의 권한 복구 기록을 먼저 읽고, 현재 앱의 실제 권한 승인을 확인한다.
- 카메라 연습·합성 테스트·실제 macOS 입력 성공을 구분해서 보고한다. 사용자의 손 좌표나 영상 기록은 커밋하지 않는다.
